apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base
namePrefix: dev-
configMapGenerator:
- name: env-configmap
  files:
  - application.properties
patchesStrategicMerge:
- statefulset.yaml